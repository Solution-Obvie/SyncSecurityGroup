Param(
    [Parameter(Mandatory = $true)]
    [String]
    $SiteUrl,
    [Parameter(Mandatory = $true)]
    [String]
    $FunctionAppName
)

#deploy propertybag
#need security group value
#need function url 

#deploy app to web site